{
  "buildCommand": "cd mobile --legacy-peer-deps && npm install && npx expo export -p web --output-dir dist",
  "outputDirectory": "mobile/dist",
  "framework": null,
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
