{
  "buildCommand": "cd mobile && npm install --legacy-peer-deps && npx expo export -p web --output-dir dist",
  "outputDirectory": "mobile/dist",
  "framework": null,
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}